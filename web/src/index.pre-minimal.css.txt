/* Archived before the minimal Tiesen dashboard redesign. */
@import "tailwindcss";

:root {
  color-scheme: dark;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  font-synthesis: none;
  text-rendering: optimizeLegibility;
  background: #090b08;
}

* {
  box-sizing: border-box;
}

html {
  min-width: 320px;
  background: #090b08;
}

body {
  margin: 0;
  min-width: 320px;
  min-height: 100vh;
}

button,
a {
  -webkit-tap-highlight-color: transparent;
}

.font-display {
  font-family: "Arial Black", "Helvetica Neue", ui-sans-serif, system-ui, sans-serif;
}

.dashboard-shell {
  position: relative;
  background:
    radial-gradient(circle at 18% -5%, rgba(132, 204, 22, .09), transparent 28rem),
    radial-gradient(circle at 90% 18%, rgba(250, 204, 21, .04), transparent 24rem),
    #090b08;
}

.dashboard-shell::before {
  position: fixed;
  inset: 0;
  z-index: 0;
  pointer-events: none;
  content: "";
  opacity: .24;
  background-image:
    linear-gradient(rgba(255, 255, 255, .012) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255, 255, 255, .012) 1px, transparent 1px);
  background-size: 48px 48px;
  mask-image: linear-gradient(to bottom, black, transparent 65%);
}

.dashboard-shell > div {
  position: relative;
  z-index: 1;
}

.hero-grid::before {
  position: absolute;
  inset: 0;
  pointer-events: none;
  content: "";
  background-image:
    linear-gradient(rgba(163, 230, 53, .025) 1px, transparent 1px),
    linear-gradient(90deg, rgba(163, 230, 53, .025) 1px, transparent 1px);
  background-size: 28px 28px;
  mask-image: radial-gradient(circle at 70% 50%, black 0%, transparent 72%);
}

.hero-glow {
  position: absolute;
  top: 50%;
  right: 4%;
  width: 28rem;
  height: 28rem;
  transform: translateY(-50%);
  border-radius: 999px;
  background: radial-gradient(circle, rgba(132, 204, 22, .12), rgba(132, 204, 22, .02) 45%, transparent 70%);
  filter: blur(4px);
}

.text-gradient {
  color: transparent;
  background: linear-gradient(100deg, #d9f99d 4%, #a3e635 45%, #fde68a 92%);
  background-clip: text;
  -webkit-background-clip: text;
}

.live-dot {
  display: inline-block;
  width: 6px;
  height: 6px;
  border-radius: 99px;
  background: #a3e635;
  box-shadow: 0 0 0 0 rgba(163, 230, 53, .55);
  animation: live-pulse 2s infinite;
}

.progress-ring {
  transition: stroke-dashoffset 1s cubic-bezier(.2, .8, .2, 1);
  filter: drop-shadow(0 0 8px rgba(163, 230, 53, .35));
}

.logo-cube {
  position: relative;
}

.logo-cube::after {
  position: absolute;
  inset: 4px;
  content: "";
  border: 1px solid rgba(17, 24, 12, .18);
  border-radius: 7px;
}

.recharts-surface:focus {
  outline: none;
}

@keyframes live-pulse {
  0% { box-shadow: 0 0 0 0 rgba(163, 230, 53, .48); }
  70% { box-shadow: 0 0 0 7px rgba(163, 230, 53, 0); }
  100% { box-shadow: 0 0 0 0 rgba(163, 230, 53, 0); }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    scroll-behavior: auto !important;
    animation-duration: .01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: .01ms !important;
  }
}
